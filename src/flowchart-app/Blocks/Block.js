import { Container } from 'pixi.js';

export default class Block extends Container {
  constructor(x, y, scale = 1) {
    super()
    this.x = x;
    this.y = y;
    // TO DO
    // mainBlock
    // connectingPoint
  }
}